# Lambda-specific requirements for AgentCore Runtime proxy
# These dependencies will be packaged with the Lambda function

# HTTP client for calling AgentCore runtime
aiohttp

# Observability
aws-opentelemetry-distro
opentelemetry-api
opentelemetry-sdk
opentelemetry-instrumentation

# AWS SDK (already available in Lambda runtime, but specifying for completeness)
boto3
botocore